type Post @model @auth(rules: [{allow: public, operations: [read, update, create]}, {allow: owner}, {allow: groups, groups: ["Admins"], operations: [delete, update, read, create]}]) {
  id: ID!
  content: String!
}
 